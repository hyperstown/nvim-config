return {
  "stevearc/aerial.nvim",
  -- AstroNvim's snapshot pins ^2.2, whose treesitter backend crashes on
  -- nvim 0.12 (iter_matches `all` option removed). v4+ targets nvim 0.12.
  version = "^4",
}
